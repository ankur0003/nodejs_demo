Built in modules

1. OS
2. Path
3. FS
4. HTTP


NPM
for local dependency - use it only on this project
npm i <packagename>

global dependency - use it in any project
npm install -g <packagename>

for example mpn i lodash
which will then have an automatic entry in package.json

package.json - manifest file (stores important info about package/project)
manual approach (create package.json in the root, create properties etc)

npm init (step by step, press enter to skip)
npm init -y (everyuthing default)